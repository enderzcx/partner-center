# 独立佣金结算首版 · 本地实现合同

2026-09-18，依据当前任务中 Ender 确认的「独立结算」「Fuji 测试闭环」及「做吧」。

## 已批准范围

独立应用拥有商家工作台、推广者收款设置和结算回执。BeefAPI 是首个接入方，既有佣金规则和原始余额继续由 BeefAPI 拥有。结算服务接收已冻结的结算单，不重复计算佣金。首版只有一个商家和一个演示推广者；页面身份切换是本机测试工作区，不作为生产身份认证。

当前阶段为 promotion-candidate 本地实现，非生产系统。x402、AA、主网、多商户、市场撮合、推广归因、退款政策变更均不在首版。公网部署、DNS、Fuji 合约发布及转账需要分别授权并配置测试签名环境，本轮不执行。生产凭据和数据禁止使用。

## 模块与写入范围

- 独立项目：本目录。Bun + SQLite 单进程、静态网页、viem EVM 接口、有限权限 ERC20 出款合约。
- BeefAPI：独立 codex/fuji-settlement 工作树，仅增加默认关闭且专用鉴权的测试适配接口与冻结/完成记录。
- 本地验证：真实本地 EVM 合约部署、测试代币转账、重试恢复、数据库事务和浏览器。
- Fuji：只实现配置和运行说明，未配置/未授权不能声称链上通过。

## 资金与恢复约束

金额在接口为整数字符串 USDC 最小单位，计算使用 bigint，账本约束金额上限。原佣金账本只允许可用余额冻结，冻结后不能消费。结算单固定收款地址、金额、来源和唯一编号。合约同编号只能支付一次。

服务在广播之前持久化已签名交易及其哈希。状态 reserved → prepared → broadcast → confirmed → completed。prepared/broadcast 遇到不明结果，保留冻结并仅重播同一原始交易；confirmed 只恢复原账本回写。明确 reverted 标为 blocked，保留冻结交人工核查，不自动换号重付或解冻。一个进程、一个独占执行钱包、串行 nonce，无后台多副本承诺。

链上完成需核对网络、合约、代币、成功 receipt、付款事件、收款地址、金额、编号和代币 Transfer。暂停阻止新签名/广播但仍核验已发出交易和补回写。

本地 fixture 来源仅合成测试佣金，用独立 SQLite 模拟原账本，不是 BeefAPI 或生产收入证据。BeefAPI HTTP 来源使用真实 global affiliate 可用余额冻结和已提现回写，是另一层独立验收。

## 本地 UI 与权限

仅监听 127.0.0.1，严格校验 Host/Origin，随机 HttpOnly SameSite cookie。拒绝公网监听。测试工作区可操作商家与演示推广者，禁止将其作为公网认证上线。钱包绑定签名包括域名、用户、随机数、链 ID、时间和过期，单次使用。仅本地链模式有测试钱包快捷绑定。

## 验收

| 场景 | 必须证据 |
|---|---|
| 正常自动结算 | 来源冻结、真实本地 EVM 到账、receipt 匹配、来源完成 |
| 相同来源重复/内容冲突 | 同一任务返回同一编号；更换金额/地址拒绝 |
| 成功后重启/回写失败 | 原交易不变，只补回写 |
| RPC 未知/资金不足/明确回滚 | 不误报成功，不释放或重复支付 |
| 冻结与消费竞争 | 同一可用收益只消耗一次 |
| 暂停 | 不发新交易，已广播继续确认 |
| 钱包变更 | 已生成单的收款地址不变 |
| 权限 | 错误令牌/跨 Origin/重放签名被拒绝 |
| UI | 桌面、375/390px 可用，无横向溢出，状态与 API 一致 |

详细命令和实际结果由 README.md 及 docs/VERIFICATION.md 记录；无证据的格子不能记为通过。

## 2026-09-18 前端修正

Ender 明确要求前端沿用海外站设计并遵守 TTU。当前设计依据、源文件和浏览器验收见 [UI-DESIGN.md](UI-DESIGN.md)。产品仍独立，资金及接口合同不变。

## 2026-09-18 Fuji 单笔授权

Ender 在确认测试资金到账后，明确回复「确认」，授权部署 Fuji Settlement 合约，并向公开地址 `0x831C5C93a221D8508ad4808C2A64D58B15f77c85` 结算 1 测试 USDC，核验到账和来源账本回写。部署者、管理员、执行钱包均为 `0x28172e0d973fFf24651B6Ed4cA6d1007bc168C94`；仅使用 Circle Fuji 测试 USDC，来源为 BeefAPI 独立临时测试库。授权不包含主网、生产余额或公网应用部署。此前的 Fuji 写入限制在本次明确单笔范围内解除。

## 2026-09-18 订单驱动演示阶段

Ender 在总览后要求「下一步吧」，本轮实现此前建议的订单驱动演示：零初始佣金、10 USD 测试订单、复用 BeefAPI 订单锁定比例与支付完成返佣逻辑、推广者签名绑定钱包、冻结实际佣金、自动付款及页面回执。支付确认是明确标注的模拟事件，不调用 Stripe 等真实渠道。测试订单能力仅在双端显式演示配置开启时可用，仍为回环地址上的测试工作区。

来源与执行账本采用配套的持久测试数据库，重启不重新赠送余额、不重复生成订单或付款。钱包须签名证明控制权；首次结算尝试固定收款地址，失败重试不得随钱包变更改地址。零比例不创建付款，订单已锁定比例不随当前比例变化。旧 fixture 与普通 BeefAPI 适配模式保持原行为。

本轮本地实现、真实本地 EVM/HTTP/浏览器验证已获授权。新的 Fuji 资金注入和付款以金额、收款地址明确的单独授权为执行条件；此前1测试USDC付款已完成，不可复用旧授权重复付款。公网、生产、主网、真实收款、x402和AA不在本轮。

### 本轮 Fuji 订单单笔授权

Ender 随后明确确认：向现有 Fuji 合约充值 1 测试 USDC，通过一笔10 USD测试订单，将实际产生的1测试USDC佣金付到 `0x831C5C93a221D8508ad4808C2A64D58B15f77c85`。仍需钱包所有者亲自在订单演示页签名绑定，不得由宿主代签或绕过绑定。此次授权不包括更多订单付款、主网或生产余额。

## 稳定对外演示版 · 2026-09-18

Ender 对“服务常驻、演示账号登录和权限隔离、部署地址与HTTPS”回复「做吧」，并将域名选择交给主会话。经 Ender 提出伙伴含义后，采用 `partner.bflabs.app`（伙伴中心）。本阶段仍为单商家/单推广者的受控 Fuji 演示，不扩展真实支付、主网或多租户产品。

受控账号 merchant 与 promoter 分工：商家创建/确认测试订单并控制出款；推广者只查看自己的收益和签名绑定钱包。公网不允许匿名会话获得数据或操作权限，不允许用前端角色切换代替服务端授权。HTTPS origin固定且只信任精确Host/Origin。密码只保存哈希，会话可过期及退出，登录有限速。原本地模式保留。

部署目标采用现有服务器的独立服务目录、持久测试数据库和独立反向代理站点。不得改变既有 BeefAPI 数据库、上游或站点路由。新的服务使用原 Fuji 合约和测试执行钱包，秘密仅由宿主管理，不进入源码、镜像或协作者上下文。迁移前停止旧Fuji执行器并核对无在途单，成对备份/迁移来源与执行账本，避免两个执行器共用nonce。此阶段不授权额外链上充值或付款；线上验证优先使用既有完成回执和无付款的登录/权限检查。

发布门槛：登录/权限负向测试、旧流程回归、容器与常驻来源测试、独立安全审查、HTTPS证书验证、匿名拒绝与两角色真实客户端读回。回滚只撤回新服务和新增站点，保留数据备份，不回滚既有BeefAPI服务。

## 公网新订单单笔验收 · 2026-09-18

Ender 补充20测试USDC后，对“先完成公网新订单实测，再推进x402”的建议回复“下一步吧”。本轮执行一笔默认10 USD测试订单、10%佣金、1测试USDC，收款为现有签名绑定的 `0x831C5C93a221D8508ad4808C2A64D58B15f77c85`；不复用此前已经消耗的单笔授权，不额外充值、不变更钱包、不接真实收款或主网。该新单授权已执行完毕。x402仍待另定接入合同。


## x402 测试收款阶段

Ender 已批准接入x402真实测试网付款流程。当前范围、恢复边界和付款人前置见 [X402.md](X402.md)，最新公网报价、真实收款与返佣验收见 [VERIFICATION.md](VERIFICATION.md)。


### x402 单笔钱包授权已完成

Ender 明确表示“我授权你做了，钱包连接好了，应该在Chrome，你可以cua全程操作”，批准现有待付10测试USDC订单及其10%自动返佣。主会话通过Chrome/MetaMask完成一次原授权签名，已收款10并返佣1测试USDC。仅恢复同一订单，无第二笔扣款。该授权现已执行完毕，不延伸为后续订单或主网支付授权。链上及页面凭证见 VERIFICATION.md 最新完成节。
