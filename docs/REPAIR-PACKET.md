# Lead-verified repair packet

Initial backend passes 23 own tests, but the lead independently reproduced behavioral gaps with real module calls. Do not weaken the existing product contract or hide gaps behind mock UI.

1. Fixture threshold/partial transfer: add two commissions 600000 each with maturity0 and auto=true: available1200000 but zero payouts. Add10000000, transfer1000000: available9000000 but zero payouts. Threshold applies to aggregate mature unspent income, not each original commission. Track remaining commission amounts consumed by transfer/reservation atomically; aggregate eligible residuals into an immutable payout (retain audit allocation). Preserve min amount and maturity; oldest or deterministic consumption; no double use.
2. Source callback HTTP404 resolves successfully in createBeefApiSource.complete. NEVER mark completed for404, malformed body, success:false, or nonmatching returned attestation. Completion must validate reservation id/request identity, completed status, txHash, chain, token, recipient, amount. Source schema is known {success:true,data:row}; no speculative envelope aliases required. Optional balance unavailability is distinct from write success.
3. Source pull persists cursor before worker imports items: crash after pull permanently loses reservations. Poll from after_id0 each sweep with local pagination, idempotent import. Do not persist cursor ahead of durable imports. Do not swallow conflict/invalid configured-user records or transport failure silently. Expose bounded source error in state and /run; keep prior data with unavailable balance markers.
4. Real BeefAPI source does not start automatically since shouldStart only uses local autoSettle, while UI hides that switch and source wallet not locally bound. Imported already-frozen BeefAPI items should settle automatically (respect pause), no /run force needed; maturity already owns source commission eligibility, don't re-gate source records by demo threshold.
5. paused prepared records skip inspect. Broadcast can land then throw before markBroadcast; status remains prepared. Pausing must still inspect these and finish if confirmed without rebroadcast. Inspect before replay, only replay pending same raw while unpaused. Re-check pause after awaits.
6. lock.ts unsafe invalid lock deletion + truncate heartbeat lets second starter unlink live lock. Replace with a small OS-backed exclusive lock, e.g. dedicated Bun SQLite lock DB holding BEGIN EXCLUSIVE for lifetime. Parent verified second connection rejects SQLITE_BUSY and releases on close. Need real child process collision, SIGKILL/restart recovery, not only fake PID files. Fail closed if corrupt lock data.
7. Bind SQLite ledger to immutable runtime fingerprint (source kind/baseURL + partner + chainId/token/contract + executor public address). Refuse reuse with different config. Source ID must incorporate request_id plus numeric record ID; test DB reset/new request should never alias old paid row. Use synchronous FULL for durable signed transaction state. Startup errors must release lock; shutdown drains pending worker before closing DB/releasing lock.
8. Don't leak raw arbitrary provider errors. sanitizeError can leak 32-byte hex private key and RPC user/password/API query. Use generic provider failure codes/messages and explicitly safe ServiceError copy; test key/opaque token/credential URL. Current CLI logs unsanitized err.message a second time: remove.
9. GET /api/state currently configured=true even balances errors; mark unavailable explicitly and expose bounded readiness error. Empty monetary string must not show zero (lead fixes UI). Reject missing JSON media type for mutation; body reading should cap streaming before allocation, not only after arrayBuffer.

Validation: add regression cases for all money/recovery flaws, plus actual scheduler/non-force source tick. Tests should call current service modules; main will rerun local EVM/HTTP integration separately. Work only owned backend files/tests in grok-service. No chain/public/BeefAPI edits, no deployment. Commit repair separately from initial work. Fixed interface remains existing UI contract; optional sourceError/network.error fields are allowed for correct unavailable states.

Lead also added a real EVM acceptance test in ../fuji-demo/tests/acceptance.test.ts. Running against f8eb8e1 failed at paused restart: expected confirmed, got prepared after actual chain payment and lost reply. Main will rerun unchanged after repair.
