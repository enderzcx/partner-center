# 海外站整套前端迁移

2026-09-18，Ender否定了只沿用样式的简化实现，明确要求：直接迁入海外站整套前端，删除无关功能，将可用页面和组件改为伙伴中心。此前codex/partner-product简化方案不发布。

本轮从BeefAPI的origin/codex-enhance提交d8a3f6d1fdb8df8c238ce723b561dca60f6bf315实际导出完整web源码，在隔离工作树内裁剪。保留React/Vite、海外首页、公共页眉页脚、登录框架、控制台与邀请中心的真实源码结构。以迁移清单和原始文件对比证明复用，不能仅用同色同字体代替。

产品入口：/、/login、/console、/console/orders、/console/settlements、/console/wallet、/docs。根据实际账号角色展示商家或推广者内容。删除国内管理、模型、API Key、充值、试用、模型调用记录等无关入口。保留上游许可和署名；BeefAPI仅作首个接入案例，产品品牌为伙伴中心，团队为BF Labs。

已验收的支付、冻结、出款、回写业务不重写。复用已有x402付款协议客户端，对接现有同源接口。允许为React构建产物添加受限静态资源和明确SPA路由；不得放宽API权限、CSP、Origin或钱包签名校验。禁止自动触发新付款、连接生产账本或部署。本地实现及测试不代表公网已升级。

验收包含：源码迁移清单、实际React构建、原有回归测试、SPA/资源安全测试、同源API集成、匿名/两角色/退出/失败恢复、桌面与手机真实浏览器截图、链接和路由核验。静态预览不能冒充完整接口验收；只读历史快照不能冒充新链上交易。


## 2026-09-19 完成记录

- Grok迁移任务20260918-234318-delegate-056136d5，作者5995613，整合7a636d1。先实际复制完整海外web目录，再裁剪为伙伴中心的React/Vite页面；旧的简化HTML方案没有参与本轮实现。原站公共页眉页脚、首页构图和动效、登录框架、侧栏、控制台、邀请中心样式继续使用。
- 主会话逐项处理残留：移除无引用的BeefAPI字标/邮件/OG图；首页回执改为2026-09-18已完成测试网案例，控制台图换成真实运行截图，页脚署名BF Labs。保护上游许可与署名。
- 根目录`bun run typecheck`、`bun test`通过：127 pass / 0 fail / 943 assertions，16 files。`bun run build:web`通过，`git diff --check`通过。Semi构建块约1.04MB未压缩/296KB gzip，保留实际组件带来的体积警告，非构建失败。
- 独立审查20260919-000619-review-075c8247针对7a636d1提出两项：无登录fixture控件缺失、React重绘前重复操作锁缺口。主会话均确认修复，审查原结论及处置见 evidence/overseas-review.json。没有把原needs-attention结论写成approve。
- 操作锁回归：暂移除同步ref赋值，三项测试均失败；恢复后，同帧普通操作、登录、x402付款均只调用一次。未发送实际付款或签名。会话flag来自真实session，覆盖state缺失字段；4323实际auth-off服务浏览器读回添加测试收益、自动结算和钱包入口可见。
- 浏览器Ego任务245：对照真实global.beefapi.com，检查迁移版1440px首页/控制台/登录/回执，390px推广者/钱包/菜单，375px结算记录，320px首页。已修复手机状态竖排、首页中文拆词、侧栏高度、回执未使用模态焦点行为。回执可Escape关闭。
- 实际服务端登录/session/退出处理用于4322预览；金额、订单和回执使用已验收历史快照，资金写接口拒绝执行。浏览器完成两角色登录、退出、页面导航、推广者访问商家订单回到自身控制台、空记录、503错误、原页面保持登录并恢复三条记录。新增静态HTTP检查覆盖明确SPA路由、构建JS/CSS、缺失文件、非法主机和API不回退为HTML。
- 运行页的错误/CSP探针在登录及商家首屏读回为空；后续以实际各页面呈现、路由和资源读回为证据。首页控制台图片正常加载，所有首页链接均指向现有页面或已知测试网交易。最终首页截图使用减少动态效果设置，避免后台页面动画暂停影响截图。
- 真实资金、签名、主网、生产账本修改均为0。仅本地提交，未推送、未构建发布镜像、未部署公网。公网仍为x402-v4。完整新页面在4322预览，不是旧4320。
- 保留codex/partner-overseas整合工作树、依赖与4322只读服务供查看/后续发布；清理作者工作树和4323临时fixture服务。截图在.local/ui-evidence，展示稿为docs/DEMO-TALK.md。
